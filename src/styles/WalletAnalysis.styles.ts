import {StyleSheet} from 'react-native';

export const walletAnalysisStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 8,
  },
  filterContainer: {
    flexDirection: 'row',
  },
  chartContainer: {
    marginTop: 24,
  },
});
