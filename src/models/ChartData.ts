export interface HistoryDataModel {
  c: number[];
  h: number[];
  l: number[];
  o: number[];
  t: number[];
  v: number[];
  s: string;
  nextTime: any;
}

export interface ChartData {
  x: number;
  y: number;
}
